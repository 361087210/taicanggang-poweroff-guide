node_modules/
__pycache__/
*.pyc
tcg_app/platforms/
tcg_app/plugins/
tcg_app/www/
archive/
tests/test_screenshots/
*.log
build_err*.txt
build_log*.txt
# V10.12: 开发机无npm的本地引导产物(见 scripts/bootstrap_npm.js), 不入库
scripts/.npm-vendor/
scripts/.npm-scratch/

# ===== V5.3.1 CI/CD规范化: 签名密钥与二进制产物不入库(安全红线) =====
# 正式产物走 GitHub Release, 内部测试产物走飞书云文件夹, 源码仓库不放二进制
release/keystore/
*.keystore
*.jks
release/*.apk
release/*.ipa
release/*.sha256
platforms/android/release-signing.properties

# =====# V10.13: 飞书同步临时产物 + 凭据本地文件
.env.feishu
vehicles_data.js.bak

# V10.15.15: 自动生成/构建产物不入库
web-data/debug_structure.json
.trae-html-share-packages/

# ===== V10.19.1: 测试运行产物不入库 =====
# tests/v1010_e2e_results.json / v1010_solutions_results.json /
# v1011_mirror_sync_results.json 由三个测试每次运行写入, 全仓无人读取
# (docs/TEST_MATRIX.md 仅把它们列为"历史结果产物")。此前被 git 跟踪,
# 导致每跑一次测试就产生无意义 diff, 淹没有意义的代码改动。
# 入库移除 + 忽略规则双管齐下; 由 tests/test_v1019_repo_hygiene.js 守卫。
tests/*_results.json
